#include <stdio.h>

/**
 * main -  display name
 *@argc: integers
 *@argv: char.
 * Return: Always (0).
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d \n", argc - 1);

	return (0);
}
