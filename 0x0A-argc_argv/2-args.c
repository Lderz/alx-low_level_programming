#include <stdio.h>

/**
 * main - shows all arguments passed into it
 * @argc: Number of command line arg.
 * @argv: Arrays 
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int x;

	for (x = 0; x < argc; i++)
		printf("%s\n", argv[i]);

	return (0);
}
